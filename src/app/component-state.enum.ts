/**
 * An enum to represent a components state. It's helpful when controlling
 * multiple design states or some functionality only available during particular
 * states.
 */
export enum ComponentState {
  Ready = 'ready',
  Loading = 'loading',
}
